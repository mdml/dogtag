name = "starter"
persona = "a fresh install"
distinguishing_axes = [
  "the normative initialization profile's own defaults",
  "lifecycle where the ordinary state is a named value",
]
corpus = "scheduled"
corpus_milestone = "M2"
