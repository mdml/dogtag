name = "starter"
persona = "a fresh install"
distinguishing_axes = [
  "the initialization profile's own defaults, unmodified",
  "lifecycle where the ordinary state is a named value",
]
corpus = "scheduled"
corpus_milestone = "M2"
