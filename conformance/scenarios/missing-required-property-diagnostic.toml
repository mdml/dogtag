id = "missing-required-property-diagnostic"
title = "A missing required property yields a stable diagnostic"
milestone = "M3"
status = "executable"
contract = """
Given a corpus that conforms except for one note omitting a property its
declared type requires.

When the corpus is validated.

Then validation reports one diagnostic with a stable identifier, addressed to
the offending note and naming the missing property, and the rest of the
corpus still validates cleanly.
"""
