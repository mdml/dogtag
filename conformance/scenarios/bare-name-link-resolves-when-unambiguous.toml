id = "bare-name-link-resolves-when-unambiguous"
title = "An unambiguous bare-name reference resolves"
milestone = "M3"
status = "pending"
contract = """
Given a corpus containing a reference written as a bare name in the profile's
declared dialect, where exactly one note in the corpus bears that name.

When the reference is resolved.

Then it resolves to that note — without path qualification and without any
precondition that names be unique across the corpus.
"""
